export function InboxPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold">Inbox</h1>
      <p className="mt-2 text-muted-foreground">
        Assets uploaded without a project. Move them into a project when ready.
      </p>
    </div>
  )
}
